name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy City
        run: .scripts/action.sh "$PASSWORD"
        env:
          PASSWORD: ${{ secrets.NEOCITY_PASS }}
